apiVersion: dcm.io/v1
kind: Environment
metadata:
  name: powerdns
  labels:
    env: prod
spec:
  provider: powerdns
  capabilities:
    - dns
  config:
    apiUrl: http://localhost:8081
    apiKey: dcm-secret
    server: localhost
