apiVersion: dcm.io/v1
kind: Environment
metadata:
  name: dev-postgres-1
  labels:
    env: dev
spec:
  provider: postgres
  config:
    kubeconfig: /Users/omachace/.kube/config-1
  capabilities:
    - loadbalancer
  cost:
    tier: standard
    hourlyRate: 0.05
